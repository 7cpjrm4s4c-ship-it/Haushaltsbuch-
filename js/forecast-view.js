/* Basis-Komposition der Prognoseansicht. Zustandslogik und Renderer liegen in eigenen Modulen. */
'use strict';

VIEW_TITLES.einstellungen='Prognose';

function vPrognose(){
  const {baseYear,ui,assumptions,startAssets,buckets,months,years,summary}=forecastData();
  const first=months[0]||{debt:0};
  const startDebt=Number(summary.startDebt??first.openingDebt??first.debt??0);
  const startNetWorth=startAssets-startDebt;
  const scenario=ForecastEngine.SCENARIOS[ui.scenarioKey]||ForecastEngine.SCENARIOS.realistic;
  return `<div class="desktop-page-title">Finanzprognose</div><div class="forecast-layout"><section class="card forecast-controls"><div class="card-title">Prognose einstellen</div><div class="form-grid two"><div class="field"><div class="lbl">Szenario</div><div class="sw"><select class="sel" onchange="setForecastOption('scenarioKey',this.value)">${forecastScenarioOptions(ui.scenarioKey)}</select></div></div><div class="field"><div class="lbl">Prognose bis</div><div class="sw"><select class="sel" onchange="setForecastOption('endYear',this.value)">${forecastYearOptions(ui.endYear,baseYear)}</select></div></div><div class="field"><div class="lbl">Variable Kosten · Durchschnitt</div><div class="sw"><select class="sel" onchange="setForecastOption('lookbackMonths',this.value)">${[3,6,12].map(n=>`<option value="${n}"${n===ui.lookbackMonths?' selected':''}>letzte ${n} Monate</option>`).join('')}</select></div></div><div class="field"><div class="lbl">Inflation variable Kosten (% p.a.)</div><input class="inp" type="number" step="0.1" min="-10" max="20" value="${ui.annualInflation}" onchange="setForecastOption('annualInflation',this.value)"/></div></div></div><div class="forecast-note">${scenario.label}: variable Ausgaben werden mit ${Math.round(scenario.variableFactor*100)} % des historischen Durchschnitts angesetzt.</div></section><section class="card forecast-controls"><div class="card-title">Startvermögen</div><div class="forecast-note">Diese Werte werden ausschließlich für die Prognose verwendet und erscheinen nicht auf dem Dashboard.</div><div class="form-grid two">${forecastAssetInputs()}</div><div class="forecast-note">Liquidität: <strong>${fmt(buckets.liquidity)}</strong> · Anlagevermögen: <strong>${fmt(buckets.investments)}</strong> · Restschulden: <strong>${fmt(startDebt)}</strong> · Nettovermögen zum Start: <strong>${fmt(startNetWorth)}</strong></div></section><section class="card forecast-controls"><div class="card-title">Rendite & Kaufkraft</div><div class="form-grid two">${forecastReturnInputs()}<div class="field"><div class="lbl">Allgemeine Inflation / Kaufkraft (% p.a.)</div><input class="inp" type="number" min="-20" max="50" step="0.1" value="${assumptions.purchasingPowerInflation}" onchange="setForecastAssumption('purchasingPowerInflation',this.value)"/></div><div class="field"><div class="lbl">Neue Sparraten fließen in</div><div class="sw"><select class="sel" onchange="setForecastAssumption('savingsTarget',this.value)">${forecastSavingsTargetOptions(assumptions.savingsTarget)}</select></div></div></div><div class="forecast-note">Renditen werden monatlich aus der Jahresannahme berechnet. Neue Sparraten erhalten Rendite ab dem Folgemonat. Die Kaufkraftinflation verändert nur den realen Vergleichswert, nicht den nominalen Kontostand.</div></section><section class="forecast-kpis"><div class="forecast-kpi"><span>Liquidität am Ende</span><strong class="${summary.endLiquidity>=0?'forecast-positive':'forecast-negative'}">${fmt(summary.endLiquidity)}</strong><small>Ende ${ui.endYear}</small></div><div class="forecast-kpi"><span>Rendite / Zinsen</span><strong>${fmt(summary.cumulativeReturns)}</strong><small>kumuliert</small></div><div class="forecast-kpi"><span>Sondertilgungen</span><strong>${fmt(summary.totalSpecialRepayments)}</strong><small>im Zeitraum</small></div><div class="forecast-kpi"><span>Nettovermögen nominal</span><strong class="${summary.endNetWorth>=0?'forecast-positive':'forecast-negative'}">${fmt(summary.endNetWorth)}</strong><small>Ende ${ui.endYear}</small></div><div class="forecast-kpi"><span>Nettovermögen real</span><strong class="${summary.endRealNetWorth>=0?'forecast-positive':'forecast-negative'}">${fmt(summary.endRealNetWorth)}</strong><small>heutige Kaufkraft</small></div><div class="forecast-kpi"><span>Niedrigste Liquidität</span><strong class="${summary.minLiquidity>=0?'forecast-positive':'forecast-negative'}">${fmt(summary.minLiquidity)}</strong><small>${forecastLowPoint(summary)}</small></div></section><section class="card"><div class="card-title">Vermögensentwicklung</div>${forecastWealthChart(months)}</section><section class="card"><div class="card-title">Jährlicher Finanzierungsspielraum</div>${forecastTimeline(years)}</section><section class="card"><div class="card-title">Jahres- und Monatsdetails</div><div class="forecast-year-list">${forecastYearDetails(years,months)}</div></section></div>`;
}
